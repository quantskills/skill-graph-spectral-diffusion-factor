# Portable Loader Prompt

Replace `<GRAPH_SPECTRAL_DIFFUSION_FACTOR_SKILL_ROOT>` with the repository root, then load the root `SKILL.md`, relevant `references/`, `schemas/`, and deterministic `scripts/`.

Use only predeclared PandaData factor channels and point-in-time graph snapshots. Require authorization before direct data access, evaluation, or writes. Do not search arbitrary fields, train graph neural networks, use future graph membership, handle credentials, or provide investment advice.
