# Overlap Review

The account-visible organization scan covered 225 repositories and 197 `skill-*` repositories at preparation time. It includes account-visible private repositories but cannot enumerate private, quarantined, unregistered, or stale assets outside that scope. Registry maintainers must perform final semantic review.

Nearest neighbors include `skill-dl-gnn-stock-graph`, `skill-concept-rotation-monitor`, `skill-rotation-radar`, `skill-capital-flow-crowding-monitor`, `skill-factor-mine`, `skill-factor-evaluate`, and `skill-market-regime-analysis`.

This Skill differs by applying fixed, auditable graph-signal filters to predeclared `get_factor` channels. It trains no GNN, does not generate TopK predictions, does not produce a rotation report, and does not search arbitrary factors or graph structures.
